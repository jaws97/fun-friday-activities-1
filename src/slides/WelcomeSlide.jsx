/* Single screen: just the title. The arrow keys move straight
   on to the next slide; joining via QR lives on the console instead. */
export function WelcomeSlide() {
  return (
    <div className="slide center">
      <div className="sl-mast">Once Upon a Wednesday</div>
    </div>
  );
}
